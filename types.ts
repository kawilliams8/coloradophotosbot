export interface ScrapedData {
  title: string;
  imageUrl: string;
  imageDate: string;
  summary: string;
  altSummary: string;
  nodeUrl: string;
}

export interface ScheduledNode {
  id: number;
  description: string;
}
